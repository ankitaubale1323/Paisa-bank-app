apiVersion: v1
kind: Service
metadata:
  name: {{ include "paisa-app.fullname" . }}
  labels:
    {{- include "paisa-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "paisa-app.selectorLabels" . | nindent 4 }}
