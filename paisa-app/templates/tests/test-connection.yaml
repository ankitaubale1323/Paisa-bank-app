apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "paisa-bank-app.fullname" . }}-test-connection"
  labels:
    {{- include "paisa-bank-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "paisa-bank-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
